name: sql-lint
on: [push, pull_request]
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Run SQLFluff
        uses: yu-iskw/action-sqlfluff@v4 # Use v4 or latest tag
        with:
          sqlfluff_version: "2.3.5"
          dialect: bigquery
          # ... other configurations like paths, formatters, etc.
